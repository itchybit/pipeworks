import {vec3} from "gl-matrix"

function create_vec3s(p){
  // const p = vec3.fromValues(p.x,p.y,p.z);
  // const n = vec3.fromValues(p.nx,p.ny,p.nz);
  // return (p, n);
}

function max_axis(v){
  // return a.reduce((iMax, x, i, arr) => x > arr[iMax] ? i : iMax, 0);
}

export function pipe(par) {
  // const {a, an} = create_vec3s(par.start);
  // const {b, bn} = create_vec3s(par.end);
  // const start_axis_i = max_i(an);
  // const end_axis_i = max_i(bn);
  // 
  // const p1 = [0,0,0]
  // const p2 = [0,0,0]
  //
  // p1[start_axis_i] = a[start_axis_i] - b[start_axis_i]
  // p2[end_axis_i] = a[end_axis_i] - b[end_axis_i]


}
